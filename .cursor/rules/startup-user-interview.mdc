---
description: 创业项目用户访谈的入口、权限、会话锁定与和知识网络/普通对话的边界
alwaysApply: true
---

# 创业项目用户访谈（early / startup）

入口文案用「用户访谈」，不要用「进件」「创业设计」。按钮例如「开始用户访谈」「继续上次没问完的访谈」。
访谈进行中：这条会话不能插入普通对话。访谈结束后：同一条会话可以普通聊。
对话里说「生成/更新知识网络」只引导去知识网络页，不要再走 Hermes 整页任务。
Plugin 阶段产出的 Markdown 进项目资料包（`documents` + 对象存储），用相对路径分目录（如 `AI生成/startup/01-discovery/`），不要只丢在「AI生成」扁平文件里。同一路径再生成是新版本。

## 谁能开、谁能答

- 未加入 / 游客：不能开、不能答。
- 管理员：能开始、暂停、结束访谈，指定回答人，发布/放弃草案。
- 成员：能对话、能看、可被指定为回答人；不能开访谈、不能发布。
- Basic：只读，不能对话、不能访谈、不能发布。
- 同一项目同一时间只允许一条进行中的访谈。
- 有未发布草案时不能开下一轮访谈。
- 访谈一结束就自动开跑「更新全部章节」出草案。

early 没有投资人/项目方协作工作台。历史上的 issuer 在 early 项目里按成员处理。

## 会话怎么锁

- 必须是独立会话（自己的 conversation id），不能靠「这句话像创业」从普通对话里自动跳进。
- 进行中：开场先读项目资料包，资料写清的题跳过。系统只按 startup-design 问法，一次 3–5 题；聊偏了就短拒并摆回未答题。约 2–3 小轮覆盖进件后摘要收工，对话里说「材料够了，开始生成知识网络」并立刻开跑更新全部章节。管理员仍可提前点「结束访谈」。不要对用户提接口/后台/系统状态，不要说「资料里找不到」。
- 访谈未结束：不要在这条会话或知识网络里开「生成全部」。问完自动结束后除外。
- 这不是协作里的「待确认问题」（那是生成之后向协作方要文件）。
- 项目不是 early 时不要出现访谈入口。
- 材料里已有的题跳过；中断后从下一题接着问；用户改口要记更正，后面阶段用新答案。
